<section id="precios">
  <div class="container">
    <div class="precios-grid">
      <h2 class="h2 precios-title">Listado de precios</h2>
      <!-- Columna izquierda -->
      <div class="precios-col">
        <article class="precio-item">
          <div class="precio-top">
            <h3>Corte de cabello</h3>
            <span class="dots"></span>
            <strong>18€</strong>
          </div>
          <p>
            Plasmamos con precisión la idea del estilo que deseas obtener, utilizando las técnicas y herramientas necesarias.
          </p>
        </article>

        <article class="precio-item">
          <div class="precio-top">
            <h3>Rapado y barba</h3>
            <span class="dots"></span>
            <strong>20€</strong>
          </div>
          <p>Rapado y perfilado de barba a tu gusto, para destacar tu estilo equilibrado.</p>
        </article>
      </div>

      <!-- Columna derecha -->
      <div class="precios-col">
        <article class="precio-item">
          <div class="precio-top">
            <h3>Corte de cabello y barba</h3>
            <span class="dots"></span>
            <strong>23€</strong>
          </div>
          <p>
            Realizamos el corte según tus especificaciones y definimos la barba conforme a tus indicaciones.
          </p>
        </article>

        <article class="precio-item">
          <div class="precio-top">
            <h3>Cejas</h3>
            <span class="dots"></span>
            <strong>5€</strong>
          </div>
          <p>Diseño y perfilado de cejas, limpieza y definición.</p>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
    /* HOME-only: Precios estilo "menú" */
  .precios-title{
  grid-column: 1 / -1;
  text-align: center;
}

  .precios-grid{
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 34px;
    padding: 26px;
    border: 1px solid var(--border);
    border-radius: 18px;
    background: var(--card);
    box-shadow: var(--shadow);
  }

  .precios-col{
    display: flex;
    flex-direction: column;
    gap: 26px;
  }

  .precio-item h3{
    margin: 0;
    font-size: 16px;
    font-weight: 800;
  }

  .precio-item p{
    margin: 10px 0 0;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.6;
  }

  .precio-top{
    display: flex;
    align-items: baseline;
    gap: 12px;
  }

  .precio-top strong{
    font-weight: 900;
    font-size: 16px;
    color: var(--text);
    white-space: nowrap;
  }

  .precio-dots{
    flex: 1;
    border-bottom: 1px dotted rgba(255,255,255,.28);
    transform: translateY(-2px);
    min-width: 20px;
  }

    .precios-grid{
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 34px;
}

/* 📱 Móvil */
@media (max-width: 900px){
  .precios-grid{
    grid-template-columns: 1fr;
  }
}
</style>