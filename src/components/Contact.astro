<section id="contacto">
  <div class="container">

    <div class="card contacto-wrapper">
      <h2 class="h2 contacto-main-title">Contacto</h2>
      <p class="lead contacto-lead">
        Rellena esto y te confirmamos por WhatsApp.
      </p>

      <div class="grid contacto-grid">
        <form class="contacto-form-wrapper">
          <div class="grid contacto-form">

            <label class="field">
              <div class="field-label">Nombre</div>
              <input class="field-control" name="nombre" required />
            </label>

            <label class="field">
              <div class="field-label">Teléfono</div>
              <input class="field-control" name="telefono" required />
            </label>

            <label class="field">
              <div class="field-label">Servicio</div>
              <select class="field-control" name="servicio">
                <option>Corte</option>
                <option>Barba</option>
                <option>Corte + Barba</option>
              </select>
            </label>

            <label class="field">
              <div class="field-label">Mensaje</div>
              <textarea class="field-control" name="mensaje" rows="4"></textarea>
            </label>

            <button class="btn primary" type="submit">
              Enviar
            </button>

          </div>
        </form>

        
          <h3 class="contacto-title">Datos rápidos</h3>

          <ul class="contacto-list">
            <li>📍 Avda Constitución N 49, Don Benito</li>
            <li>📞 <a href="tel:+34657774315">+34 657 77 43 15</a></li>
            <li>✉️ <a href="mailto:hola@canobarber.com">hola@canobarber.com</a></li>
          </ul>

          <div class="divider"></div>

          <a class="btn contacto-wa" href="https://wa.me/34657774315" target="_blank">
            <img src="/whatsapp.png" alt="WhatsApp" class="whatsapp-icon" />
            Abrir WhatsApp
          </a>
      </div>
    </div>

  </div>
</section>

<style>
  /* Layout */
.contacto-grid{
  grid-template-columns: 1fr 1fr;
  margin-top: 18px;
  align-items: start;
  gap: 18px;
}

.contacto-card{
  padding: 18px;
}

/* Form */
.contacto-form{
  grid-template-columns: 1fr;
  gap: 12px;
}

.field-label{
  font-size: 13px;
  margin-bottom: 6px;
  color: var(--muted);
}

.field-control{
  width: 100%;
  padding: 12px 12px;
  border-radius: 14px;

  border: 1px solid rgba(97,33,15,.22);
  background: rgba(255,243,219,.65);
  color: var(--darkBrown);

  outline: none;
  transition: border-color .15s ease, box-shadow .15s ease, background .15s ease;
}

.field-control::placeholder{
  color: rgba(97,33,15,.45);
}

.field-control:focus{
  border-color: rgba(97,33,15,.45);
  box-shadow: 0 0 0 4px rgba(97,33,15,.12);
  background: rgba(255,243,219,.85);
}

textarea.field-control{
  resize: vertical;
  min-height: 110px;
}

.contacto-hint{
  margin: 0;
  font-size: 12px;
}

/* Right card */
.contacto-title{
  margin: 0 0 10px;
}

.contacto-list{
  margin: 0;
  padding-left: 18px;
  color: var(--muted);
  display: grid;
  gap: 8px;
}

.contacto-list a{
  color: var(--brown);
  text-decoration: none;
  font-weight: 600;
}

.contacto-wa{
  width: 100%;
  justify-content: center;
}


.contacto-wrapper{
  padding: 26px;
}

.contacto-main-title{
  margin: 0 0 6px;
}

.contacto-lead{
  margin-bottom: 20px;
}

@media (max-width: 900px){
  .contacto-grid{
    grid-template-columns: 1fr;
  }
}

/* FORM sin caja */
.contacto-form-wrapper{
  padding: 0;
}

/* Icono WhatsApp más grande y sin filtro marrón */
.whatsapp-icon{
  width: 18px;
  height: 18px;
  display: block;
  filter: none; /* importante */
  margin: 6px;
}
</style>